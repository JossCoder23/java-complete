## Quiz

1. If multiple conditional operators are placed in a single expression, which operator gets evaluated first?
    * An expression cannot contain more than one conditional operator.
    * The OR operator - ||
    * The AND operator - &&
    * ```The NOT operator - !```

2. The AND logical operator is represented in Java by which operator?
    * ||
    * ```&&```
    * !
    * and

3. What will the following code print?
    ```java
    if (5 > 6) {
        System.out.println("C. Senor");
    } else if (6 < 5) {
        System.out.println("Sue Yu");
    } else {
        System.out.println("Jim Sox");
    }
    ```
    * ```Jim Sox```
    * C. Senor
    * Sue Yu
    * A blank line.

4. What will the following code print?
    ```java
    int choice = 5;

    switch (choice) {
    case 4: 
        System.out.println("Jo Fraizer");
        break;
    case 5: 
        System.out.println("Laila A. Lee");
        break;
    case 6: 
        System.out.println("Hall E. Home");
        break;
    default: 
        System.out.println(200);
    }
    ```
    * ```Laila A. Lee```
    * 200
    * Hall E. Home
    * Jo Fraizer

5. The following Boolean expression will be evaluated to what value?
    ```java
    !(false) && (5 > 4)
    ```
    * (5 > 4)
    * ```true```
    * false
    * unknown

6. The OR logical operator is represented in Java by which operator?
    * &&
    * ```||```
    * !
    * OR

7. The ```break``` keyword is important in a ```switch``` statement due to which of the following?
    * Without it, the switch statement will not execute.
    * Java will return an error if it is not specified.
    * ```Without break, Java will continue to check other cases in the switch statement.```
    * It is necessary whenever the switch statement contains more than 4 cases.