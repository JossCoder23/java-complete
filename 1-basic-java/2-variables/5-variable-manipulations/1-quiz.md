## Quiz

1. Which operator can be used to concatenate two Strings?
    * ```+``` (this)
    * ```.equals()```
    * ```*```
    * ```-```

2. What will the following program output?
    ```java
    int num = 12;
    num *= 2;
    num -= 4;
    num++;
    System.out.println(num);
    ```

    * ```21```
    * 12
    * 9
    * 20

3. What is the best way to tell if the following two Strings are equal?
    ```java
    String username1 = "teracoder";
    String username2 = "gigacoder";
    ```
    * username1==username2
    * username1.isEqualTo(username2)
    * ```username1.equals(username2)```
    * System.out.println(username1)

4. The expression 5 != 6 will evaluate to what value?
    * 6
    * 5
    * ```true```
    * false

5. True or False: The value of a variable declared with the final keyword can be changed after its initial declaration.
    * True
    * ```False```

6. How could we get a result of ```10```, given the following variable?
    ```java
    double a = 2;
    ```
    * ```a % 10```
    * ```a / 2```
    * ```a * 5``` (this)
    * ```a - 12```

7. What does the following code do?
    ```java
    System.out.println(8 <= 8);
    ```
    * Prints 8
    * ```Prints true```
    * Prints 0
    * Prints false

8. Are there any errors in this Java statement?
    ```java
    int status = 7 < 8;
    ```
    * There are no errors.
    * Yes, there should be no semicolon.
    * ```Yes, int should be boolean.```
    * Yes, int should be char.

9. To what value does the following string concatenation evaluate?
    ```java
    "It's " + 5 + "pm"
    ```
    * Error
    * "It's pm"
    * ```"It's 5pm"```
    * 11

10. After the following code is run, what value will the variable endpoint be assigned to?
    ```java
    int endpoint = 11 % 3;
    ```
    * 2.66
    * ```2```
    * 1
    * 11