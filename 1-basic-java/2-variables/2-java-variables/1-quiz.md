## Quiz

1. What datatype can only be assigned one of two values?
    * double
    * char
    * int
    * ```boolean```

2. Which line declares the variable ```bestProgrammingLanguage``` and initializes it to be ```"Java"```?
    * ```String bestProgrammingLanguage = "Java";```
    * "Java" = String bestProgrammingLanguage;
    * string bestProgrammingLanguage = "Java";
    * bestProgrammingLanguage = String "Java";

3. What line of code declares a variable called ```numRabbits``` to store a whole number?
    * numRabbits int;
    * number numRabbits;
    * numRabbits = int;
    * ```int numRabbits;```

4. Which option is a valid variable name and follows the Java naming conventions?
    * 2ndPhoneNumber
    * TimeUntilLaunch
    * second_phone_number
    * ```timeUntilLaunch```

5. Which of the following lines would throw a compilation error?
    * String gradeOnTest = "A";
    * char grade_on_test = 'F';
    * ```double isRaining = false;```
    * int balance = -30;

6. What is the value of num?
    ```java
    int num = (10 - (4 + 3)) * 6;
    ```
    * ```18```
    * 20
    * -32
    * 24

7. Which one of the following values is a valid ```char```?
    * 7
    * "a"
    * ```'F'```
    * 'ab'

8. What value CANNOT be assigned to a variable with the datatype double.
    * 5
    * 6.7
    * -.2
    * ```"60"```