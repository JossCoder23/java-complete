## Quizz

1. True/False: concat() doesn’t actually change the value of the original string.
    * False.
    * ```True.```

2. Which of the following string methods returns the string value converted to uppercase?
    * ```toUpperCase()```
    * indexOf()
    * toLowerCase()
    * charAt()

3. Suppose we have a ```String``` like so. How would we use ```substring()``` to print out a value of ```940```?
    ```java
    String address = "940 Lorimer";
    ```
    * address.substring(1, 3)
    * ```address.substring(0, 3)```
    * address.substring(0, 2)
    * address.substring(4)

4. Complete the code below to find the character located at index 2:
    ```java
    String str = "qwer";

    System.out.println( __________ . __________ ( __________ ));
    ```
    * indexOf
    * ```charAt```
    * ```str```
    * ```2```

5. What will the following code print out?
    ```java
    String message = "how r u?";  

    System.out.println(message.length());   
    ```
    * ```8```
    * 5
    * 6
    * 7