## Quiz

1. What will the following code print to the screen?
    ```java
    public class HelloYou {
        public static void main(String[] args) {
            System.out.println("Hello Codey!");
        }
    }
    ```
    * ```Hello Codey!```
    * This code contains an error.
    * "Hello Codey!"

2. In Java, what is the purpose of leaving comments in code?
    * ```They provide human readable notes that clarify thinking.```
    * They are only present in compiled code.
    * They provide checks that the compiler must pass.
    * They are how words are printed to the screen.

3. What is missing from this Java program?
    ```java
    public class LanguageFacts {
        // Covers the history of the Java programming language
    }
    ```
    * A single-line comment.
    * The line to compile code: javac LanguageFacts.java
    * The curly braces that mark the scope of the class.
    * ```The main() method: public static void main(String[] args) {}```

4. Comment the code with the correct syntax.
    ```java
    public class Timeline {
        public static void main(String[] args) {

            ______ Sun Microsystems announced the release of Java in 1995
            
            ______
            James Gosling is a Canadian engineer who created Java while
            working at Sun Microsystems. His favorite number is the
            square root of 2!
            ______

        }
    } 
    ```
    * \\
    * ```/*``` (second)
    * ```//``` (first)
    * */*
    * ```*/``` (three)
    * \*\

5. What would the name of the file be if it contained the following code?
    ```java
    public class HelloWorld {
        public static void main(String[] args) {
            System.out.println("Hello World!");
        }
    }
    ```
    * ```HelloWorld.java```
    * HelloWorld.class
    * There are no restrictions on file name in Java.

6. Java is a compiled language, meaning the code we write is translated by another program into a language the computer understands.
    * ```True```
    * False

7. The following code will run without an error.
    ```java
    public class LanguageFacts {
        public static void main(String[] args) {    
            System.out.println("Java is a class-based language.");
            System.out.println("Java classes have a 'main()' method.");
            System.out.println("Java statements end with a semicolon.")
        }
    }
    ```
    * ```False```
    * True